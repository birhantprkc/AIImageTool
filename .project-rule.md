---
project_name: zero_vision
domain: computer_vision_and_image_processing
primary_language: csharp
target_frameworks: ["net8.0-windows"]
architecture: universe_plugin_v4
framework_references:
  agent_option_root: "E:\\16. AgentOption"
  rules: ["R_CORE", "R_UNIVERSE", "R_CS", "R_WPF", "R_PERF", "R_GIT"]
  shortcuts: ["BASE", "PERF", "DARK", "CTOK", "UI_WPF"]
key_paths:
  core: "ImageTool.Core/"
  imaging: "ImageTool.Imaging/"
  host: "ImageTool.Host/"
  plugins: "ImageTool.Plugins.*/"
  shared: "ImageTool.Shared/"
  tests: "ImageTool.Tests/"
  solution: "ImageTool.slnx"
  publish_script: "publish.ps1"
---

# ZeroVision Project Directives
- **Develop Pipeline:** Maintain non-destructive 32-bit float Linear Light color space for all image filters and processing operations.
- **Universe Architecture:** Decouple AI vision modules (Upscaler, FaceRestorer, VisionTagger) into autonomous plugins communicating via IPC (`ImageTool.Shared`).
- **Hardware Acceleration:** Leverage DirectML / ONNX Runtime in background host process (`ImageTool.Host`) to prevent UI thread blocking.
- **UI & Theming:** Strictly adhere to C# WPF Desktop UI & Theming Standards (`[AgentOption]/standards/csharp/wpf-ui.md`).
- **Zero Memory Leaks:** Ensure proper native bitmap buffer recycling and GPU memory management across editing operations.
